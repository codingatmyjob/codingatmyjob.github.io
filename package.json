{
  "name": "codingatmyjob-site",
  "version": "0.1.1",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "predeploy": "npm run build",
    "deploy": "git add . && git commit -m 'Update' && git push origin main && npm run build && git checkout -f gh-pages && git checkout main -- dist && mv dist/* . && rm -r dist && git add -A && git commit -m 'Deploy' && git push origin gh-pages && git checkout main"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^5.1.0",
    "gh-pages": "^6.3.0",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "vite": "^7.1.12"
  }
}
